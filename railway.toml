# API (Docker) — full notes: railway/api.toml. Duplicated here so Railway can auto-detect at repo root. Web app: frontend/railway.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
