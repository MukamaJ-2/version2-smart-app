# API service: use with repo root as service root (not `backend/`).
# Start command comes from Dockerfile (gunicorn). Web app: use /frontend/railway.toml (set in service settings).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
