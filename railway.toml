# API (Docker) — full notes: railway/api.toml. Duplicated here so Railway can auto-detect config at repo root.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
